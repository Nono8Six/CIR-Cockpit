# UI/UX — changelog

Journal des travaux de la remise à niveau UI/UX. Le plan, les checkpoints et les
prompts vivent dans `docs/UI_UX/plan-refonte-ui.md` ; ce fichier ne contient que
l'historique.

## Règles d'écriture

- Une ligne par tâche traitée, ajoutée **en haut** du tableau (le plus récent en
  premier).
- La date est celle du jour du travail, au format `JJ/MM/AAAA`.
- La colonne « Tâche » porte l'identifiant exact du plan (`T1.1`, `T3.6`…), ou
  `—` pour une entrée hors plan.
- La colonne « Statut » vaut `✅ terminée`, `🟡 partielle` ou `↩️ annulée`.
- La colonne « Preuve » nomme une preuve vérifiable : une commande et son
  résultat, un chemin de capture, un nom de test, un identifiant de commit.
  « Fait » n'est pas une preuve.
- Une tâche partielle décrit dans « Changement » ce qui reste ouvert.
- Ne jamais réécrire une ligne existante : une correction ajoute une ligne.

## Journal

| Date | Tâche | Statut | Changement | Preuve |
| --- | --- | --- | --- | --- |
| 31/07/2026 | T2.4 | 🟡 partielle | Correction de la ligne précédente après arbitrage PO : la navigation ne s'affiche plus à l'ouverture. Objection retenue — une barre de recherche qui s'ouvre sur les 7 sections double visuellement le menu de gauche et démarre sur un menu au lieu d'une recherche. Non retenu en revanche : supprimer la navigation, le doublon entre menu, `F1`–`F8` et palette étant une seconde modalité clavier normale (Linear, Ramp, Attio, Stripe) et non une redondance nuisible, et la règle PO du skill `cir-cockpit-design` désignant la palette comme entrée privilégiée pour la navigation globale. `visibleCommands` dans `AppSearchOverlay.tsx` ne retourne donc que le groupe `creation` tant que la requête est vide et non préfixée ; les 7 sections reviennent derrière `>` (mode commande explicite) ou dès qu'une frappe libre les nomme, « pilot » remontant `Pilotage F4`. Le critère « atteindre n'importe quelle section sans souris » reste tenu, l'écran d'accueil redevient récents + `Créer`. | `pnpm -w run qa:front` → `EXIT=0` : `Test Files 162 passed (162)`, `Tests 785 passed (785)`, `Error compliance check passed.` (31/07/2026, 11:01) ; test « affiche les recents et les 7 sections avant toute frappe » remplacé par deux tests qui figent l'arbitrage — « ouvre sur les recents et les creations, jamais sur un doublon du menu » (assertion négative sur les 7 `app-search-command-navigation-*`) et « expose les 7 sections derriere le prefixe > » ; relevés `innerText` en direct sur `http://localhost:3000` : ouverture = `… RÉCENTS \| AQUITAINE ELECTRIQUE \| SEA \| PONTAC Thierry \| Créer \| Nouvelle interaction …` sans bloc `Aller à`, `>` = `… Aller à \| Clients \| Tiers \| F1 \| … \| Paramètres \| Admin \| F8 \| Créer …`, `pilot` = `… Aller à \| Pilotage \| Interactions \| F4` |
| 31/07/2026 | T2.4 | 🟡 partielle | Palette `Ctrl K` passée de la recherche seule au motif palette de commandes. **Registre de commandes :** nouveau `frontend/src/app/appCommands.ts`, dont la navigation est *dérivée* de `buildShellNavigation` plutôt que recopiée — les 7 sections (Clients F1, Fournisseurs F2, Saisie F3, Pilotage F4, Référentiels CIR F5, Admin F7, Paramètres F8) et leur raccourci suivent donc automatiquement le shell, et « Fournisseurs / Admin / Paramètres » disparaissent pour un TCS sans branche `if` supplémentaire. Les 3 actions de création pointent uniquement vers des parcours qui existent déjà, vérifiés dans `app/router.tsx` : `Nouvelle interaction` → `/cockpit` (même destination que la CTA du sidebar), `Créer un client ou un prospect` → `clientsCreateRoute` `/clients/new`, `Créer un fournisseur` → `supplierCreateRoute` `/suppliers/new` (super admin / admin agence seulement). Rien n'a été inventé : aucune commande « archiver », « exporter » ou « inviter » n'existe côté produit, donc aucune n'est proposée. **Récents :** `InteractionSearchRecents.tsx` est réutilisé tel quel et rendu au-dessus de la liste tant que rien n'est tapé, alimenté par `searchData.recentEntities` qui existait déjà mais n'était consommé que par la saisie. Deux corrections étaient nécessaires pour que la promesse tienne : l'état `loading` de l'index d'entités masquait l'écran initial (il n'est plus évalué qu'à requête non vide) et la requête n'était pas remise à zéro à la fermeture, si bien que la palette rouvrait sur l'ancienne frappe, donc sans récents. **Préfixes :** `!` est abandonné (`useAppSearchData.ts`) au profit de `&` pour les clients — signe neutre, non porteur d'urgence ou de négation, et accessible sans modificateur sur AZERTY — et `>` est ajouté pour les commandes. Les quatre préfixes sont montrés trois fois : dans les chips de filtre (qui affichent désormais le signe en `Kbd` au lieu d'un caractère décoratif), en légende permanente dans le pied de dialogue, et dans le placeholder de l'entrée. La table `SCOPES_BY_PREFIX` remplace la cascade de trois `if`. **État vide :** la phrase grise devient trois sorties réellement actionnables au clavier — créer une fiche pour la requête saisie, élargir aux archivées, effacer le filtre — la seconde branchée sur le paramètre `includeArchived` de `useEntitySearchIndex`, qui existait mais était figé à `false` ; le drapeau est remonté dans `useAppViewState`/`useAppQueries`, doublé d'un chip « Archivés inclus » réversible, et remis à `false` à chaque fermeture pour que le périmètre par défaut reste prévisible. **Clavier :** défaut trouvé en rejouant le parcours — `Échap` renvoyait le focus sur `BODY`. Deux causes cumulées, la palette démontée à la fermeture (`isSearchOpen ? … : null`) et l'absence de restauration explicite ; elle reste maintenant montée après la première ouverture et `CommandDialog` accepte un `onCloseAutoFocus` qui refocalise le déclencheur mémorisé. **Faute d'accent :** déjà corrigée par T1.5, rien à reprendre. **Reste ouvert :** captures pixel non produites (volet navigateur non compositant), remplacées par des relevés DOM ; et le `Ctrl N` affiché par la CTA du sidebar n'est lié à aucun raccourci réel dans `useAppShortcuts` — non traité ici, la commande de création ne l'affiche donc pas. | `pnpm -w run qa:front` → `EXIT=0` : `Repo state check passed.`, typecheck OK, lint `--max-warnings 0` OK, `Test Files 161 passed (161)`, `Tests 783 passed (783)`, `Error compliance check passed.` (31/07/2026, 10:33) ; tests ajoutés dans `frontend/src/components/__tests__/AppSearchOverlay.test.tsx` (« affiche les recents et les 7 sections avant toute frappe », « atteint une section au clavier sans souris », « reserve la liste aux commandes derriere le prefixe > », « propose une sortie quand la recherche ne donne rien », « explique chaque prefixe dans le pied de dialogue »), `frontend/src/app/__tests__/useAppSearchData.test.tsx` (« ne traite plus le point d exclamation comme un filtre ») et `frontend/src/hooks/__tests__/useAppViewState.test.tsx` (« garde la palette montee et repart a neuf a chaque fermeture », « ouvre les parcours de creation existants depuis la palette ») ; parcours rejoué sur `http://localhost:3000` en 1280×720 : `>pilot`+`↓`+`Entrée` → `{"url":"/dashboard","dialogOpen":false}`, commande de création → `{"url":"/clients/new?type=all&…"}`, `Échap` → `{"active":"BUTTON|Ouvrir la recherche rapide"}` contre `"BODY|"` avant correction ; relevés des 4 états dans `scratchpad/palette-etats.txt` |
| 31/07/2026 | T2.3 | ✅ terminée | Administration des utilisateurs ramenée à de la lecture plus une seule voie d'action. **Densité :** les 3 cartes KPI de `UsersManagerContent.tsx` (3 pastilles d'icônes décoratives, `rounded-2xl`, `text-2xl`, ombres portées et `hover:-translate-y-0.5` pour trois nombres à un chiffre) sont supprimées au profit d'une ligne `font-mono text-[11px] tabular-nums` collée au titre — « 3 utilisateurs · 3 actifs · 0 archivé », avec accord singulier/pluriel et `skeleton-shimmer` tant que la requête n'a pas répondu. Le reste s'aligne sur `/remises/referentiels` : header et boutons en `h-8 … text-xs`, recherche en `density="dense"` bornée à `max-w-72`, cadre de table `rounded-xl border-border-subtle bg-card`, en-tête `sticky`. **Élévation de privilège :** le select inline sans libellé disparaît de la cellule Rôle, qui devient du texte (`ROLE_LABELS`, `font-medium` sur Super admin) ; le changement passe par le nouveau `UserRoleChangeDialog.tsx` (AlertDialog) qui nomme l'utilisateur et son email, l'ancien rôle, le nouveau, et affiche `ROLE_PERMISSIONS[nouveauRôle]` — trois résumés ajoutés dans `app/appConstants.tsx` et vérifiés sur le code de gating réel (`App.tsx:86-88`, `canImport = super_admin`), pas inventés. La confirmation reste inerte tant que le rôle n'a pas changé, et une alerte dédiée apparaît sur toute montée vers Super admin. Côté état, `handleRoleChange(userId, role)` est remplacé par `openRoleChangeDialog` / `executeRoleChange`, de sorte que la mutation `setUserRole` n'est plus atteignable depuis la ligne : le dialog est le seul appelant. **Grammaire d'action :** recommandation du prompt suivie sans divergence — les 4 affordances par ligne tombent à 2, la case à cocher (qui a une destination) et le menu ⋮, qui gagne « Modifier le rôle » et conserve « Gérer les agences ». Le bouton « Modifier » de la cellule Agences disparaît avec son composant `controls/UserMembershipPills.tsx`, supprimé ; les agences deviennent un texte tronqué avec `title`. Le déclencheur ⋮ reçoit un nom accessible par ligne (`Actions pour <email>`) au lieu d'un « Actions » répété trois fois. `UserCard.tsx` (vue mobile) est réduit de la même façon et reçoit le menu de la liste via `renderActions`, pour que les deux vues ne puissent pas diverger. **Sélection :** la barre d'actions groupées existait déjà et fonctionne ; elle est conservée et redensifiée (`h-7`, `text-[11px]`, `shadow-soft` au lieu de `shadow-2xl`). **Hauteur :** le scroll descend de `TabsContent` vers la table, l'onglet Utilisateurs passant en colonne flex bornée et la zone de défilement étant désormais interne au cadre. **Écart mineur assumé :** l'animation `animate-ping` du statut « Actif » est retirée, une pastille clignotante permanente sur chaque ligne étant du bruit décoratif dans une table de lecture. | `pnpm run qa:front` → `exit=0` : `Repo state check passed.`, typecheck OK, lint `--max-warnings 0` OK, `Test Files 161 passed (161)`, `Tests 775 passed (775)`, `Error compliance check passed.` (31/07/2026, 09:51) ; tests ajoutés dans `frontend/src/components/users/__tests__/UsersManagerList.test.tsx` — « ne laisse qu une seule voie d action par ligne », « passe le changement de role par le menu et jamais par la ligne », « traite les etats chargement, erreur et vide », « nomme l utilisateur, les deux roles et ce que le nouveau autorise » — et dans `frontend/src/hooks/__tests__/useUsersManager.test.tsx` — « ne change aucun role tant que le dialog de confirmation n a pas ete ouvert », « applique l elevation de privilege et nomme les deux roles apres confirmation » ; assertion de chaîne de hauteur mise à jour dans `frontend/src/components/__tests__/AdminPanel.test.tsx`. **Captures pixel non produites** : le volet navigateur ne composite pas (`screenshot failed: the Browser pane is not displayed`) et l'exécution de JS en page a été refusée par le classifieur ; remplacées par des relevés `read_page` avant/après sur `http://localhost:3000/admin` en 1440×900 (avant : `checkbox` + `combobox` sans nom + `button "Modifier"` + `button "Actions"` par ligne, en-tête `TOTAL 3 / ACTIFS 3 / ARCHIVÉS 0` ; après : `checkbox` + `button "Actions pour <email>"`, en-tête `3 utilisateurs · 3 actifs · 0 archivé`) et par le dialogue rejoué en direct, détaillé dans la « Preuve réelle » de T2.3 |
| 30/07/2026 | T2.2 | ✅ terminée | Parcours de saisie `/cockpit` ramené à un seul dispositif de progression. **Progression :** le stepper de `CockpitGuidedEntry.tsx` devient la seule source — chaque étape porte son libellé *et* la valeur choisie (`Canal / Téléphone`), et redevient un `<button>` qui rappelle `flow.editStep` pour revenir sur une étape franchie ; les étapes non franchies restent `disabled`. Le récapitulatif disparaît avec son composant `CockpitGuidedAnswerRow.tsx`, supprimé ; `getVisibleGuidedAnswerSteps` survit et alimente désormais les valeurs du stepper, ce qui garde ses tests. Le bloc « ÉTAPE n » disparaît : `eyebrow` devient optionnel dans `CockpitGuidedQuestionFrame.tsx` et n'est plus passé par aucune étape guidée — `Étape 3` cède la place au seul titre `Rechercher ou créer le tiers`, et les étapes Canal et Relation, qui n'avaient qu'un eyebrow, reçoivent un vrai titre interrogatif (« Par quel canal avez-vous échangé ? », « Quelle relation avec ce tiers ? »). **Rail droit :** il n'est plus rendu sur un simple libellé de contexte (`selectedEntityMeta` seul ne suffit plus), il cesse de s'étirer sur toute la colonne (`self-start` + carte bornée `max-h-full`) et les trois actions remontent sous la fiche au lieu d'être poussées en bas par `mt-auto` ; elles gagnent un libellé visible (`Fiche`, `Appeler`, `Écrire`) et une infobulle qui explique l'indisponibilité (« Aucun numéro renseigné pour ce contact »), l'infobulle étant portée par un wrapper puisqu'un bouton désactivé n'émet pas de survol. **Capitales :** ne subsistent que les eyebrows de section (`NOUVELLE INTERACTION`, `RECHERCHE`, `IDENTITÉ`, `INTERACTIONS DU CLIENT`) ; passent en casse phrase avec contraste de poids/couleur les libellés du stepper, `Recommencer`, `Inclure archivés`, les champs de la fiche client (`Localisation`, `Compte`, `Pays` ; `SIREN`/`SIRET`/`NAF` restent capitales parce que ce sont des acronymes) et les huit labels de l'étape Validation via `refinedLabelStyle`. La puce `VALIDATION` du pied de page, quatrième écho de la position, est retirée. **Bouton non prêt :** traité comme un état à part entière et non comme un primaire délavé — variante `secondary` bordée sur les `Continuer` des étapes Tiers, Contact et Sujet ainsi que sur `Enregistrer` désactivé (`disabled:opacity-100`), et le raccourci `Ctrl Entrée` / `Ctrl ↵` n'est plus affiché tant que l'action ne valide rien. **Mono :** retirée du détail de contact d'`EntityContactRow.tsx` (« Directeur · 05 58 47 56 95 ») et de la ligne d'interaction du rail, où seule la date reste en `font-mono tabular-nums`. Les deux `motion.button` « Continuer » dupliqués de `CockpitGuidedDetailsQuestion.tsx` sont fusionnés en un seul élément. **Écart assumé :** l'assertion « Continuer affiche Ctrl Entrée » sur une étape incomplète, dans `CockpitGuidedStepSwitch.test.tsx`, encodait exactement le défaut n° 4 ; elle est inversée plutôt que conservée. | `pnpm run qa:front` → exit 0 : `Repo state check passed.`, typecheck OK, lint `--max-warnings 0` OK, `Test Files 160 passed (160)`, `Tests 763 passed (763)`, `Error compliance check passed.` (30/07/2026, 19:37) ; tests ajoutés dans `frontend/src/components/cockpit/guided/__tests__/CockpitGuidedEntry.test.tsx` — « porte la valeur choisie dans le stepper sans recapitulatif ni bloc etape », « permet de revenir sur une etape franchie depuis le stepper », « laisse les etapes non franchies non cliquables » — et assertion d'état neutre dans `frontend/src/components/cockpit/__tests__/CockpitShortcutLegend.test.tsx` ; parcours des 6 étapes rejoué dans Chrome sur `pnpm --dir frontend run dev` en 1440×900, captures dans `scratchpad/shots/` (`before-1-canal.png` … `before-6-validation.png`, `after-1-canal.png` … `after-6-validation.png`) ; rail droit mesuré en page à l'étape Contact : hauteur 404 px, espace résiduel 30 px, plus grand écart interne 48 px, contre 463 px de vide avant |
| 30/07/2026 | T2.1 | 🟡 partielle | Pilotage refondue autour d'une seule question, « qu'est-ce que je dois traiter maintenant ? ». **Dédoublonnage :** les trois panneaux `DashboardPriorityQueue.tsx`, `DashboardPipelineSummary.tsx` et `DashboardTopClients.tsx` sont supprimés ; ce qu'ils portaient devient les colonnes `Échéance`, `Étape` et `Montant` d'une table unique. Le modèle passe de `filterDossiersForTable` (liste d'`Interaction`) à `buildDossierRows` + `selectDossierRows` (`DossierRow`), qui produit une ligne et une seule par dossier en y attachant son urgence, son étape, son montant et sa dernière activité ; la table est triable sur 4 colonnes (`priority`, `client`, `stage`, `amount`, inversion au second clic via `toggleSort`) et filtrable par périmètre (`À traiter` / `Toute la période`) et par canal. Les actions « relance faite » et « +2 j » de l'ex-file de priorité survivent en actions de ligne au survol. `buildTopClients`/`TopClientEntry` et `myDayView` deviennent morts et sont retirés. **Métriques :** 5 cartes hétérogènes → une bande de 4 cellules `MetricCell` strictement identiques (pastille de tonalité, libellé, valeur, précision) ; plus aucune sparkline, puisque les 4 métriques ne peuvent pas toutes en porter une ; la 5ᵉ carte (« Taux conversion », dont la valeur réelle était un tiret cadratin) est supprimée. **Graphique :** rendu seulement si `hasEnoughEvolutionPoints`, qui exige ≥ 8 semaines chiffrées **et** ≥ 3 valeurs distinctes — le second critère va au-delà de l'énoncé et se justifie sur les données réelles, où la série vaut 4 800 € constants sur 12 semaines : 12 points, mais un seul fait, donc rien à tracer. La courbe est masquée et remplacée par le delta chiffré `stable sur 4 sem.` (`buildOpenDossiersDelta`) dans la métrique « Dossiers ouverts ». Le tracé conservé gagne axe Y gradué, grille discrète, points terminaux accentués et infobulle au survol. **Couleurs :** `quote_sent` quitte `bg-warning` pour `bg-foreground/70` ; `--warning`, `--success` et `--destructive` ne subsistent que sur échéance du jour, gagné, perdu et retard. **Troncature :** `shortenBadgeLabel` coupe à la source sur frontière de mot et supprime les mots-outils terminaux, `title` portant le libellé complet — « Attente éléments du client » → « Attente éléments… » et non « Attente éléments du… ». **Grille :** les trois systèmes empilés laissent place à une colonne unique en-tête / bande de métriques / table étirée à scroll interne, en-tête de colonnes collant et pied de table portant la légende des pastilles et les raccourcis clavier. `DashboardSkeleton` d'`AppMainStateView.tsx` réaligné sur cette structure (`skeleton-shimmer`), états vide et erreur traités. **Reste ouvert :** la case « zone morte ≤ 120 px » n'est pas cochée — ≈ 65 px à 1280×720, mais ≈ 250 px à 1440×900 avec les 7 dossiers réels, la table étant volontairement étirée jusqu'au pied comme celle de `/remises/referentiels` qui sert de référence de densité ; combler ce vide supposerait de réintroduire un panneau ou de déplacer le vide sur le fond de page, arbitrage PO requis. | `pnpm run qa:front` → exit 0 : `Repo state check passed.`, typecheck OK, lint `--max-warnings 0` OK, `Test Files 160 passed (160)`, `Tests 762 passed (762)`, `Error compliance check passed.` (30/07/2026, 18:45) ; tests ajoutés dans `frontend/src/utils/dashboard/__tests__/dashboardOverview.test.ts` — « produit exactement une ligne par dossier et qualifie son urgence », « ajoute les dossiers clos de la periode sans jamais dupliquer une ligne », « refuse une serie plate : douze fois le meme montant reste un seul fait », « ne termine pas un libelle raccourci sur un mot-outil » — et dans `frontend/src/hooks/__tests__/useDashboardState.test.tsx` — « filtre la table par canal et n y laisse aucun doublon », « inverse le tri au second clic sur la meme colonne », « masque la courbe tant que la serie hebdomadaire est trop courte » ; captures avant/après en 1440×900 et 1280×720 produites par Playwright sur `pnpm --dir frontend run dev` dans `scratchpad/shots/` (`avant-1440x900.png`, `avant-1280x720.png`, `apres-1440x900.png`, `apres-1280x720.png`) |
| 30/07/2026 | T1.8 | ✅ terminée | Cause trouvée avant correction, et elle n'est pas dans le schéma : `useInteractionHandlers.ts:40` (`handleSelectEntity`) remet `contact_id` à vide avec `{ shouldValidate: true }` au moment où l'utilisateur choisit le tiers à l'étape 3 ; RHF n'écrit alors l'erreur que du champ nommé, mais ce champ est justement `contact_id`, donc « Contact requis » est déjà dans `formState.errors` quand l'étape 4 se monte, et `CockpitClientContactSection.tsx:73` le rend sans condition. **Déclencheur retenu : la tentative de passage à l'étape suivante** — clic sur « Continuer », `Ctrl/⌘ + Entrée`, ou soumission du formulaire. `useCockpitGuidedFlow` gagne `revealStepErrors(step)` et `areStepErrorsVisible` (remis à zéro par `completeStep`, `editStep` et `resetFlow`, donc un retour en arrière remasque) ; `CockpitGuidedEntry` substitue un objet `errors` vide dans les deux panneaux tant que l'étape active n'a pas été tentée, ce qui couvre Canal, Relation, Tiers, Contact et Sujet en un seul point plutôt qu'en six composants. Le message « Au moins une famille produit est requise » de l'étape Sujet, qui souffrait du même défaut sans venir de Zod, passe sous le même drapeau. Les boutons « Continuer » ne sont plus `disabled` : un bouton grisé n'explique pas pourquoi, le clic révèle désormais l'erreur et appelle `focusFirstInvalidField`, qui réutilise le `useInteractionInvalidHandler` déjà en place pour la soumission et pose le focus sur le premier champ fautif (`contactSelectRef` pour `contact_id`). **Écarts assumés :** le `blur` par champ n'est pas câblé — honorer « blur sur un champ *renseigné* » demandait de croiser `touchedFields` avec la valeur de chaque champ dans six sous-composants, disproportionné face au déclencheur retenu qui satisfait les critères ; l'étape Validation garde le comportement actuel, elle n'est atteinte que complète et l'erreur de soumission doit y rester visible ; deux tests existants ont dû changer d'attente (« Continue disabled until complete » et l'assertion `toBeDisabled()` du cas familles produits), le critère « les tests existants passent toujours » entrant en contradiction directe avec le critère « après un clic sur Continuer avec un formulaire incomplet ». Capture non produite (aucune session navigateur), remplacée par les tests. | `pnpm run qa:front` → exit 0 : `Repo state check passed.`, typecheck OK, lint OK, `Test Files 160 passed (160)`, `Tests 746 passed (746)`, `Error compliance check passed.` ; 5 tests ajoutés — `CockpitGuidedEntry.test.tsx` (« n affiche aucune erreur au montage de l etape contact », « affiche l erreur et demande le focus apres un clic sur Continuer »), `CockpitGuidedStepSwitch.test.tsx` (« revele les erreurs et donne le focus au premier champ fautif au clic sur Continuer », « affiche le message familles produits une fois les erreurs de l etape revelees »), `useCockpitGuidedFlow.test.tsx` (« masque les erreurs a l arrivee sur une etape et ne les revele qu apres une tentative ») ; régression prouvée en neutralisant le masque dans `CockpitGuidedEntry.tsx` → `Tests 1 failed \| 5 passed (6)`, `Contact requis` présent au montage |
| 30/07/2026 | T1.7 | ✅ terminée | Deux textes de remplissage supprimés du rendu. **Rail droit de l'assistant de création :** le panneau « Notes de parcours » n'est plus rendu du tout, et la prop `footerMessage` comme son calcul à quatre branches disparaissent de `EntityOnboardingDialog.tsx` et `EntityOnboardingSidebar.tsx`. Décision prise après lecture du rail sur les quatre étapes : il porte déjà une aide réelle et spécifique là où il a quelque chose à dire — résumé entreprise, établissement sélectionné, signaux, dirigeants, finances en étape `company` ; checklist des champs manquants ou « Tous les champs requis sont saisis » en étape `details` ; liste des doublons cliquables ; bloc `role="alert"` pour `stepError`. Les quatre phrases étaient soit une paraphrase de ce que le rail montre déjà (« Sélection et doublons visibles avant création. »), soit, en étape 1, du remplissage pur. À noter : `footerMessage` valait `stepError ?? …`, donc l'erreur d'étape était affichée deux fois dans le même rail, une fois en alerte et une fois en citation italique ; l'alerte est conservée. Rien n'est ajouté en étape 1, où le rail garde son seul état vide légitime (« Sélectionnez une entreprise pour voir son intelligence commerciale »). **Badge de comptage :** le `'…'` + `'résultats'` littéraux laissent place à un `skeleton-shimmer` (`h-3 w-6 sm:w-16`, `aria-hidden`) dans un conteneur `aria-busy` avec libellé de chargement, hauteur de pastille inchangée ; l'accord singulier/pluriel existant est conservé une fois le total connu. Le même défaut, non cité par le constat, a été trouvé au balayage sur `admin-suppliers/AdminSuppliersPage.tsx:146` et corrigé à l'identique. **Non corrigé, signalé :** les `placeholder="Ex: EDF SAS"`, `"Ex: contact@edf.fr"`, `"Ex: 552081317"` des wizards fournisseurs (`create-wizard/supplier-details-step.tsx`, `search-step/search-filters-form.tsx`) — ce sont des attributs `placeholder` d'input, donc une aide de saisie et non du contenu rendu, mais ils exposent une entreprise réelle en exemple ; arbitrage PO requis avant de les neutraliser. Captures avant/après non produites, aucune session navigateur lancée. | `pnpm run qa:front` → exit 0 : `Repo state check passed.`, typecheck OK, lint OK, `Test Files 159 passed (159)`, `Tests 735 passed (735)`, `Error compliance check passed.` ; `grep -rn "footerMessage" frontend/src` → 0 résultat ; `grep -rnE ">\s*(\.\.\.\|…)\s*<\|'(\.\.\.\|…)'" frontend/src` → seule l'ellipsis de pagination `DirectoryTablePagination.tsx:99` subsiste ; `grep -rn "lorem\|ipsum" frontend/src` → 0 résultat |
| 30/07/2026 | T1.6 | ✅ terminée | Vouvoiement retenu comme forme unique (arbitrage PO du prompt : forme majoritaire, 20 contre 11, public professionnel) et documenté en une ligne dans `.agents/skills/cir-cockpit-design/SKILL.md` §Product-Owner Rules. Le constat annonçait 11 formulations au tutoiement ; le balayage au mot entier (impératifs 2ᵉ pers. sing., pronoms `tu`/`ton`/`ta`/`tes`/`toi`, formes `-toi`, inversions `as-tu`) en a trouvé **17 sur 16 emplacements**, soit 6 de plus que la liste du prompt : `EntityOnboardingDialog.tsx:531` (« si tu fermes »), `EntityOnboardingReviewStep.tsx:171` (« Assure-toi »), `useOnboardingNavigation.ts:62` et `:94-95` (3 messages d'erreur d'étape, seul fichier `.ts` concerné et donc invisible au critère `--type tsx`), `CockpitClientContactSection.tsx:60`, `CockpitSupplierLookup.tsx:191` et `search-step/search-results-list.tsx:84`. Les 17 sont converties. Cinq ne sont pas de simples conjugaisons, la phrase ayant été relue : « Sélectionne un établissement **de** la liste » → « **dans** la liste » ; « si tu fermes maintenant ce **flux** » → « si vous fermez maintenant ce **parcours** », aligné sur le titre du dialog « Quitter le parcours ? » ; « Renseigne **l identité** » → « Renseignez **l’identité** », apostrophe manquante restaurée ; et deux irréguliers, « passe » → « passez » et « poursuis » → « poursuivez ». **`shared/schemas` : aucune modification, vérifié et non supposé** — 0 occurrence de `tu`/`ton`/`ta`/`tes`/`toi` sur les 33 fichiers et aucun impératif 2ᵉ pers. sing. ; les messages Zod y sont impersonnels/nominaux (« Jeton upload requis. »), donc déjà compatibles, et les 3 correspondances du balayage sont des participes passés (`verifie`, `selectionne`). Aucun écran n'a pu être observé en navigateur (pas de session lancée) ; la preuve visuelle est remplacée par les matchers de test. | Critère d'acceptation : `rg "as-tu\|Choisis \|Saisis \|Sélectionne \|Renseigne \|Vérifie \|Complète " frontend/src -g '*.tsx'` → **0 ligne, exit 1**, sans même un faux positif ; balayage résiduel élargi (`ts` + `tsx`, pronoms + impératifs + `-toi`) → 1 seule ligne, faux positif assumé `pricing-references-formatters.ts:106 complete_valid: 'Complète valide'` (adjectif de libellé de statut) ; `pnpm run qa:front` : lint/typecheck OK, `Test Files 159 passed (159)`, `Tests 735 passed (735)`, `Error compliance check passed.`, exit 0 ; non-régression portée par 2 matchers — `CockpitSolicitationLookup.test.tsx:161` aligné sur « Saisissez un numéro », et `EntityOnboardingDialog.test.tsx:656` qui attendait déjà `/sélectionnez un établissement pour voir les infos du site/i` dans le worktree sale et **échouait avant cette tâche** faute de la conversion source, désormais vert |
| 28/07/2026 | T1.5 | ✅ terminée | Accents restaurés sur les chaînes rendues à l'écran de `frontend/src` : 228 règles de remplacement en quatre passes scriptées (173 + 37 libellés multi-mots, 18 libellés d'un seul mot, 5 éditions ciblées), plus 12 règles alignant les matchers de test. Les 10 occurrences du constat sont couvertes, dont « Définit le parcours… à collecter », « Intention de création », « Qualification légère », « Société », « établissements et contrôle CIR », « sans entité », « Retour aux résultats », « Gestion des accès et rôles globaux » et « Commencez à taper pour rechercher ». La passe systématique s'appuie sur trois détecteurs rejoués jusqu'à épuisement : liste de ~280 formes non accentuées, auto-cohérence (mot accentué ailleurs dans le repo mais rendu nu ici), et libellés d'un seul mot que les deux premiers manquaient faute d'espace — ce troisième a rattrapé `Societe`, `Prenom`, `Telephone`, `Role`, `Creer`, `Reessayer`, `Departement`, `Etablissement`, `Ferme`, `Previsualiser`. Les 73 chaînes contenant `ou` / `a` / `sur` ont été relues une par une (homographes corrects). **Non corrigé car ce sont des données, pas du code — nettoyage PO requis :** les libellés de statut d'interaction saisis en base (Paramètres → Statuts interactions), dont le « Termine » signalé ; l'état distant n'a pas pu être lu (MCP Supabase non authentifié dans cette session). Laissés nus volontairement aussi : les tokens de comparaison normalisés `constants/relations.ts:43` et `utils/dashboard/dashboardAggregates.ts:7-8`, comparés à des libellés de base après `normalize('NFD')`. Le mapping de catégorie `constants/statusCategories.ts` est du code et passe bien à `À traiter` / `Terminé`. Captures non produites (aucune session navigateur), remplacées par les matchers de test. | `pnpm run qa:front` : lint/typecheck OK, `Test Files 158 passed (158)`, `Tests 731 passed (731)`, `Error compliance check passed.`, `exit=0` ; non-régression prouvée par 12 matchers d'accessibilité qui échouaient tant que les libellés n'étaient pas corrigés, dans `AppSearchOverlay.test.tsx`, `ChangePasswordScreen.test.tsx`, `EntityOnboardingDialog.test.tsx`, `ClientDirectoryFilters.test.tsx`, `ClientDirectoryDetailPage.test.tsx` et `PricingReferencesPage.test.tsx` |
| 28/07/2026 | T1.4 | 🟡 partielle | Plancher typographique fixé à 11 px : les 51 `text-[8px]` / `text-[9px]` / `text-[9.5px]` de 19 fichiers passent tous à `text-[11px]`, avec compensation de densité par le poids (`font-bold`/`font-extrabold` → `font-semibold`) et le tracking (`tracking-wider`, `tracking-widest`, `tracking-[0.12em]`, `tracking-[0.14em]` → `tracking-wide` ou `tracking-[0.04em]`), de sorte que la pastille de catégorie du cockpit ne gagne que 4,6 px de large. Trois pastilles numériques à hauteur fixe montent de `size-4` à `size-5` (compteur de filtres fournisseur, compteur de filtres onboarding, jalons de `EntityRecordWizardProgress`) et le badge de mapping du dialog d'import passe de `h-4` à `h-5` — sans changer la hauteur des lignes qui les portent. Plancher documenté dans `.agents/skills/cir-cockpit-design/SKILL.md` §Visual Direction. Reste ouvert : la colonne Statut du tableau Dossiers est fixée à 110 px et sa troncature, déjà présente à 9 px, s'aggrave (169 px de texte pour 96 px utiles) — élargir `GRID_TEMPLATE` sort du périmètre ; et 8 composants dépendants de données absentes en local (aside fournisseur étapes 2-3, dialog de détail segment, dialog d'import, sidebar et résumé d'onboarding, carte tiers et lookup fournisseur du cockpit, pied de recherche interaction) n'ont pas pu être observés en vrai, seulement mesurés par injection. | `rg 'text-\[(8\|9\|9\.5)px\]' frontend/src` retourne 0 ligne (exit 1) ; `pnpm run qa:front` : lint/typecheck OK, `Test Files 158 passed / Tests 731 passed`, `Error compliance check passed.` ; audit DOM `getComputedStyle` en navigateur sur `http://localhost:3000` (1280×720) sur 6 écrans chargés — `/cockpit` étape 3, `/dashboard`, `/suppliers/new`, `/clients/new`, `/settings` → Statuts, `/remises/referentiels` — donnant `clippedCount: 0` partout et aucun texte sous 11 px hors dette `text-[10px]` préexistante ; non-régression de hauteur mesurée par réapplication à chaud des anciennes valeurs (nav de progression 38 → 38 px, ligne Dossiers 47,4 → 47,4 px) et par injection dans le build Tailwind réel (pastille statut 70,7 → 75,3 px de large à 20 px de haut, `TableHead` segment 24 px inchangé, cellule coefficient 45,5 → 45,0 px) |
| 28/07/2026 | T1.3 | 🟡 partielle | Arbitrage PO : option 2 retenue pour l'ambre, et revert de `--input`. L'ambre est dédoublé — `--warning` (`39 78% 49%`, `#de9a1b`) reste l'aplat/pastille, le nouveau `--warning-strong` (`38 85% 31%`, `#92610c`, équivalent `amber-800`) devient la couleur de texte : 2,40 → 5,33 sur `card` et 4,92 sur le fond réel des badges `bg-warning/10` (`#fcf5e8`). `warning.strong` exposé dans `tailwind.config.cjs`, 25 occurrences de `text-warning` → `text-warning-strong` dans 20 fichiers. `--input` reverté 53 % → 88 % après second rejet PO sur capture : les boutons `outline` (« Vues », « Affichage », « Assistant IA », « Suivant ») ressortaient avec un contour bien plus sombre que le reste de l'écran. Conséquence assumée et documentée : aucune bordure ne satisfait WCAG 1.4.11, il n'existe pas de valeur qui y arrive sans le rendu refusé — la case correspondante du checkpoint reste ouverte volontairement (5/6). | Vérifié en navigateur sur `http://localhost:3000` : `getComputedStyle` donne `--input: 36 12% 88%`, bordure des boutons `outline` `rgb(228, 225, 221)`, `text-warning-strong` `rgb(146, 97, 12)` ; `pnpm run qa:front` : lint/typecheck OK, `Test Files 158 passed / Tests 731 passed`, `Error compliance check passed.` ; `designTokensContrast.test.ts` porté à 20 cas |
| 28/07/2026 | T1.3 | 🟡 partielle | Correction de la ligne précédente après revue PO sur capture d'écran : le rendu était rejeté. Revert de `--border` 53 % → 88 % (`#968a79` → `#e4e1dd` : ce token alimente le `* { border-color }` global, chaque filet de table et contour de carte devenait un trait gris franc), de `--warning` 32 % → 49 % (`#916512` → `#de9a1b` : un ambre ne peut pas tenir 4,5:1 sur blanc sans virer au brun) avec `--warning-foreground` remis à `36 62% 17%`, et de `--border-subtle` 85 % → 95 % par cohérence. Conservé : `--destructive` 47 %, `--success` 33 %, `--muted-foreground` 43 % (écarts imperceptibles, seuil atteint) et `--input` 53 %, désolidarisé de `--border` — il ne sert que sur 15 contrôles réels (boutons `outline`, `select`), là où WCAG 1.4.11 s'applique, et améliore leur lisibilité sans toucher aux tables. Reste ouvert : `warning` en texte 10–11 px (2,40), `border` et `border-subtle` sur les filets décoratifs ; trois options documentées dans le plan, décision PO requise. | Rendu vérifié en navigateur sur `http://localhost:3000` (écran Référentiels CIR) avant/après revert ; `pnpm run qa:front` : lint/typecheck OK, `Test Files 158 passed / Tests 729 passed`, `Error compliance check passed.` ; `designTokensContrast.test.ts` porté à 18 cas avec `KNOWN_BELOW_THRESHOLD` qui fige les trois dettes |
| 28/07/2026 | T1.3 | 🟡 partielle | Tokens sémantiques recalés sur WCAG AA dans `frontend/src/index.css`, luminosité seule (teintes, saturations et `--primary: 6 72% 45%` inchangés) : `--warning` 49 % → 32 % (2,40 → 5,15 sur `card`), `--destructive` 60,2 % → 47 % (3,76 → 5,03), `--success` 37 % → 33 % (4,13 → 5,05), `--muted-foreground` 45 % → 43 % (4,31 → 4,65 sur `surface-2`, le pire des quatre fonds), `--border` et `--input` 88 % → 53 % (1,28 → 3,32). `--warning-foreground` passe de `36 62% 17%` à `0 0% 98%` : le texte foncé ne tient plus sur l'ambre foncé (2,39 → 4,94). Reste ouvert : `--border-subtle` remonté de 95 % à 85 % (1,09 → 1,36) mais pas à 3:1 — l'y amener exigerait L ≤ 52,5 %, soit plus foncé que `--border`, ce qui inverse les deux niveaux de trait ; séparateur décoratif, hors périmètre WCAG 1.4.11 selon l'analyse, décision PO requise. Les ~900 classes Tailwind brutes ne sont pas touchées (autre tâche). | `pnpm run qa:front` : lint/typecheck OK, `Test Files 158 passed / Tests 728 passed`, `Error compliance check passed.` ; test ajouté `frontend/src/components/ui/__tests__/designTokensContrast.test.ts` (17 cas) ; régression prouvée en remettant les 8 anciennes valeurs : `Tests 8 failed \| 9 passed (17)` |
| 28/07/2026 | T1.2 | ✅ terminée | Suppression définitive sortie de la barre d'actions des fiches annuaire (client, prospect, fournisseur — un seul composant `ClientDirectoryRecordActionsBar.tsx`, le `client-detail/ClientDetailHeader.tsx` cité par le plan étant du code mort). Elle passe par un `⋮` (`ClientDirectoryRecordDangerMenu.tsx`, item destructif en dernière position) puis un `AlertDialog` qui exige la saisie du nom exact de la fiche ; le texte annonce ce qui tombe (contacts, cascade DB) et ce qui subsiste (interactions détachées si la case est décochée), vérifié dans `dataEntitiesDelete.ts` et les contraintes FK de `20260203120000_entities_refactor.sql`. Reste ouvert hors périmètre en-tête : la suppression fournisseur depuis la liste `admin-suppliers/AdminSuppliersTable.tsx`, déjà derrière un menu mais sans saisie du nom. Capture non produite (pas de session super admin en navigateur), remplacée par des tests. | `pnpm run qa:front` : lint/typecheck OK, `Test Files 157 passed / Tests 711 passed`, `Error compliance check passed.` ; test ajouté `frontend/src/components/client-directory/__tests__/ClientDirectoryRecordActionsBar.test.tsx` |
| 28/07/2026 | T1.1 | ✅ terminée | Contrôles morts du shell supprimés : la cloche de notifications est retirée de `AppHeader.tsx` (aucune destination notifications n'existe dans le code, hors périmètre d'en créer une) ; le sélecteur d'agence du sidebar devient un `DropdownMenu` branché sur `onAgencyChange`/`agencyMemberships` remontés depuis `AppLayout.tsx`, et un `<div>` non interactif (sans chevron ni hover) quand l'utilisateur n'a qu'une agence. Changement d'agence de « Mon compte » inchangé. Capture non produite (pas de session navigateur), remplacée par un test. | `pnpm run qa:front` : lint/typecheck OK, `Test Files 156 passed / Tests 708 passed`, `Error compliance check passed.` ; test ajouté `frontend/src/components/app-sidebar/__tests__/AppSidebarContent.test.tsx` |
| 26/07/2026 | — | ✅ terminée | Création du plan de refonte : 6 phases, 31 tâches indépendantes, un prompt autonome par tâche, checkpoints et preuves attendues. | `docs/UI_UX/plan-refonte-ui.md` ; `pnpm run qa:docs` passe |
| 26/07/2026 | — | ✅ terminée | Audit UI/UX initial : 9 écrans parcourus sur l'application locale (1440×900 et 1280×720), mesures `ripgrep` sur `frontend/src` hors tests, ratios de contraste calculés dans le navigateur depuis les variables CSS résolues. 34 constats dont 9 bloquants. | Artifact d'audit du 26/07/2026 |
